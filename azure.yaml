# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: lab-gateway

# Azure Functions deployment configuration
# Note: Aspire + Azure Functions + azd is still in preview
# This configuration deploys to Azure Container Apps with Functions runtime
services:
  api:
    project: ./src/API
    language: dotnet
    host: containerapp
    docker:
      path: ./src/API/Dockerfile
      context: .
